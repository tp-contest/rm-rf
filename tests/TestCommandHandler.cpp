//
// Created by d on 08.04.19.
//

#include "gtest/gtest.h"
#include "../include/CommandHandler.h"
#include "../include/UserFile.h"
#include <stdexcept>
#include <iostream>

/*TEST(CommandHandlerSuit, addDocument) {
    CommandHandler commandHandler = CommandHandler();
    UserFile userFile = UserFile();
    //commandHandler.addDocument(userFile);
    //ASSERT_EQ(1, commandHandler.getDocumentNumber());
}

TEST(CommandHandlerSuit, editDocument) {
    CommandHandler commandHandler = CommandHandler();
    //ASSERT_THROW(commandHandler.editDocument(1), std::invalid_argument);
}

TEST(CommandHandlerSuit, editDocument1) {
    CommandHandler commandHandler = CommandHandler();
    UserFile userFile = UserFile();
    //commandHandler.addDocument(userFile);
    //ASSERT_NO_THROW(commandHandler.editDocument(0));
}

TEST(CommandHandlerSuit, deleteDocument) {
    CommandHandler commandHandler = CommandHandler();
    //ASSERT_THROW(commandHandler.deleteDocument(1), std::invalid_argument);
}

TEST(CommandHandlerSuit, deleteDocument1) {
    CommandHandler commandHandler = CommandHandler();
    UserFile userFile = UserFile();
    //commandHandler.addDocument(userFile);
    //ASSERT_NO_THROW(commandHandler.deleteDocument(0));
}

TEST(CommandHandlerSuit, pushDocument) {
    CommandHandler commandHandler = CommandHandler();
    //ASSERT_THROW(commandHandler.pushDocument(1), std::invalid_argument);
}

TEST(CommandHandlerSuit, pushDocument1) {
    CommandHandler commandHandler = CommandHandler();
    UserFile userFile = UserFile();
    //commandHandler.addDocument(userFile);
    //ASSERT_NO_THROW(commandHandler.pushDocument(0));
}*/